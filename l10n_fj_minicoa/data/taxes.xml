<?xml version="1.0" encoding="UTF-8"?>
<odoo>
  <!-- Taxes are defined in data/template/account.tax-fj_minicoa.csv via the chart template.
       Rates are kept up to date by the TaxCore sync (sync_tax_rates_from_taxcore).
       Do not add hardcoded tax records here — it causes duplicates on install/update. -->
</odoo>
